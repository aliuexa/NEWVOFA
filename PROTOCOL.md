# Vofa 通信协议

## 概述

上位机与下位机通过串口（UART6）通信，包含两种数据方向：

| 方向             | 用途                    | 格式       |
| ---------------- | ----------------------- | ---------- |
| 下位机 → 上位机 | 一帧数据（波形 + 参数） | 统一数据帧 |
| 上位机 → 下位机 | 在线调参指令            | 文本命令   |

---

## 1. 统一数据帧（下位机 → 上位机）

下位机每 1ms 发送一帧。一帧由**两段拼接**而成：

```
[JustFloat 段] [Parameter 段]

┌─ 波形数据 ─┬── 帧尾 ──┐
| f32 × N   | 00 00 80 7F |
└────────────────────────┘
            ┌── name\0 + f32 重复 ──┬── 帧尾 ──┐
            | kp\0 1.5 kd\0 0.5 ... | 00 00 90 7F |
            └─────────────────────────────────────┘
```

两段数据在 `sendFrame()` 内拼接到同一个缓冲区，通过**一次 HAL_UART_Transmit（阻塞）** 发送：

```
excuteFunctions() → sendFrame() → 延时 1ms → 下一帧
                       │
                       ├─ memcpy JustFloat + tail
                       ├─ memcpy 每个参数的 name\0 + f32
                       ├─ 追加 Parameter tail (00 00 90 7F)
                       └─ HAL_UART_Transmit (一次调用)
```

### 1.1 JustFloat 段

| 项   | 说明                                                          |
| ---- | ------------------------------------------------------------- |
| 数据 | N 个 `f32`（小端序），N = `BindFunction()` 绑定的函数数量 |
| 帧尾 | `00 00 80 7F`                                               |
| 长度 | N × 4 + 4 字节                                               |

示例（CH1=1.5, CH2=-0.3）：

```
00 00 C0 3F  9A 99 99 BE  00 00 80 7F
└─ 1.5 ─┘  └─ -0.3 ─┘  └── tail ──┘
```

### 1.2 Parameter 段

| 项   | 说明                                                                             |
| ---- | -------------------------------------------------------------------------------- |
| 数据 | `参数名\0` + `f32`，重复 M 次，M = `AddParameterListener()` 注册的参数数量 |
| 帧尾 | `00 00 90 7F`                                                                  |

示例（kp=1.5, kd=0.5）：

```
6B 70 00  ── 00 00 C0 3F ──  6B 64 00  ── 00 00 00 3F ──  00 00 90 7F
 k p \0      1.5              k d \0      0.5              tail
```

### 1.3 完整一帧示例

```
绑定 2 个函数、注册 kp/kd 两个参数：

JustFloat 段: 1.5  -0.3   00 00 80 7F
Parameter段:   kp\0  1.5   kd\0  0.5   00 00 90 7F

↓ 字节流 ↓

00 00 C0 3F  9A 99 99 BE  00 00 80 7F
6B 70 00    00 00 C0 3F    6B 64 00    00 00 00 3F    00 00 90 7F
```

---

## 2. 命令格式（上位机 → 下位机）

上位机通过 "Set" 按钮发送在线调参指令。

### 格式

```
参数名:新值\r\n
```

- 冒号 `:` 分隔参数名和值
- 换行结尾（支持 `\r`、`\n` 或 `\r\n`）
- 值以 `strtof` 解析为 `float`
- 参数名、值的前后空格会被自动 trim

### 处理流程

```
上位机 Send → "kp:1.8\r\n"
                ↓ 串口
下位机 UART6 Rx → HandleCommandLine
  → 按 name 匹配参数 → 更新 value → 触发 callback(&newValue)
```

---

## 3. 帧尾字节速查

| 段        | 帧尾 hex        | 说明                                              |
| --------- | --------------- | ------------------------------------------------- |
| JustFloat | `00 00 80 7F` | `0x7F` 在 f32 中为 NaN 高位，正常数据几乎不出现 |
| Parameter | `00 00 90 7F` | 区别于 JustFloat 段尾，`90` 标识参数段          |

---

## 4. 注意事项

- 所有发送在 `sendFrame()` 内一次 `HAL_UART_Transmit`（阻塞）完成，两段拼在同一缓冲区
- `AddParameterListener()` 的 `parameterName` 必须是**字符串常量**，传入局部变量会导致野指针崩溃
- Parameter 段在无注册参数时不会发送
- 上位机解析线程按字节顺序匹配帧尾，先遇到者先解析，两段帧尾不同保证了不会混淆
